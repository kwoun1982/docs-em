Thin determinate progress for indexing / embedding / eval runs. Teal by default; switch tone for degraded runs.

```jsx
<ProgressBar value={64} showLabel />
<ProgressBar value={92} tone="warn" />
<ProgressBar indeterminate showLabel />
```

`indeterminate` animates a sliding sliver for unknown-duration work.
