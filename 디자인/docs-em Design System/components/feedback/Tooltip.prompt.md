Lightweight hover/focus tooltip. Wrap any trigger.

```jsx
<Tooltip label="외부 호출 0건 · 100% 로컬">
  <StatusPill tone="ok" indicator="icon">local</StatusPill>
</Tooltip>
```

`side`: top (default) / bottom / left / right.
