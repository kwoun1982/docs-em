Small mono token for IDs, models, corpora, and metadata. Square corners distinguish it from the pill-shaped StatusPill.

```jsx
<Tag icon="cpu">qwen2.5-7b</Tag>
<Tag tone="slate" icon="folder">policy_2024</Tag>
<Tag tone="signal">top_k=5</Tag>
<Tag onRemove={() => drop(id)}>chunk_0427</Tag>
```
