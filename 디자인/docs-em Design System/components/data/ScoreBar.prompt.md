Horizontal score meter that colors itself on the red→green eval scale. Optional `threshold` tick marks the pass line.

```jsx
<ScoreBar value={0.912} />
<ScoreBar value={0.71} threshold={0.8} />
<ScoreBar value={0.64} max={1} showValue={false} />
```
